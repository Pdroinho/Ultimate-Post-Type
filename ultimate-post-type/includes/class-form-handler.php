<?php
/**
 * Este arquivo foi intencionalmente esvaziado.
 * A lógica de manipulação de formulários e exclusão foi movida para a classe
 * UPT_Ajax (includes/class-ajax.php) para centralizar
 * as interações de front-end que acontecem via AJAX.
 */
if ( ! defined( 'ABSPATH' ) ) exit;

class UPT_Form_Handler {
    
    public static function init() {
        // Nenhuma ação é necessária aqui, pois a lógica de exclusão por URL
        // foi substituída pela lógica de exclusão via AJAX em class-ajax.php
    }
}
